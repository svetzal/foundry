# Tarpaulin configuration for code coverage
# https://github.com/xd009642/tarpaulin

[report]
# Output formats
out-type = ["Html", "Lcov"]
# Write coverage artifacts into a gitignored directory
output-dir = "coverage"

[run]
# Timeout for test execution (in seconds)
timeout = "300s"
