[package]
name = "foundry-core"
description = "Compatibility shim — re-exports the Foundry SDK (foundry-sdk). Prefer depending on foundry-sdk directly; this crate exists to avoid a flag-day rename and will be removed in a future release."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true

[dependencies]
foundry-sdk = { workspace = true }

[lints]
workspace = true
